services:
  db:
    restart: always
    image: postgres
    volumes:
      - ./data/db:/var/lib/postgresql/data
    env_file:
      - .env
    ports:
      - ${POSTGRE_SQL_PORT}
  web:
    build: .
    command: ["./entrypoint.sh"]
    volumes:
      - .:/app
    ports:
      - ${WEB_PORT}
    env_file:
      - .env
    depends_on:
      - db
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
  celery:
    build: .
    volumes:
      - .:/code
    env_file:
      - .env
    depends_on:
      - db
      - redis
